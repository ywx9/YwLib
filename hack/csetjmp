#pragma once // clang-format off
#include "abc.h"

#define setjmp _setjmp

extern "C" {
__ywlib_begin;

typedef struct alignas(16) SETJMP_FLOAT128 {
  unsigned long long Part[2];
} SETJMP_FLOAT128, jmp_buf[16];

int __cdecl setjmp(jmp_buf);
[[noreturn]] void __cdecl longjmp(jmp_buf, int) noexcept(false);

__ywlib_end;
}
#if not __ywlib_is_imported
__ywlib_export namespace std {

using __ywlib_global jmp_buf;
using __ywlib_global longjmp;
using __ywlib_global setjmp;

}
#endif
